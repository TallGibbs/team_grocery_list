name = "team-grocery-list"
pages_build_output_dir = "."
compatibility_date = "2025-06-01"

# Workers KV binding used by functions/api/items.js.
# The `id` below is a placeholder. After creating the KV namespace
# (see README.md), replace it with the real namespace id.
[[kv_namespaces]]
binding = "GROCERY_KV"
id = "REPLACE_WITH_KV_NAMESPACE_ID"
